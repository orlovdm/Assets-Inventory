@model Assets_Inventory.Models.Asset



<script>
    $('#AddAsset').on('show.bs.modal', function (event) {
        var button = $(event.relatedTarget)
        var id = button.data('whatever')

        var modal = $(this)
        modal.find('#Id').val(id)
    })
</script>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
